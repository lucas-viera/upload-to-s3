{
  "name": "upload-to-s3",
  "version": "1.0.0",
  "description": "project to test uploads to an s3 bucket using node js ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "lucas-viera",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1400.0",
    "dotenv": "^16.3.1"
  }
}
